MONGODB_URI=mongodb://localhost:27017/posts-comments-manager
PORT=3000
JWT_SECRET=change_this_in_real_env
JWT_EXPIRES_IN=1d

# Opcional: restringir CORS (ej: http://localhost:4200). Si no se define, se aceptan todos los orígenes.
# CORS_ORIGIN=http://localhost:4200

